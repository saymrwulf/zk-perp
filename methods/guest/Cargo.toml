[package]
name = "zk-perp-guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "^3.0.4", default-features = false, features = ["std"] }
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"

# Core types for the DEX
zk-perp-core = { path = "../../crates/core", default-features = false }
